exp_name: create_index
output_dir: /home/tanmayg/Data/gpv/coco_exp/
exp_dir: ${output_dir}/${exp_name}

subset: test
tasks: 
  - coco_vqa
  - coco_captioning
  - coco_detection
  - coco_classification

defaults:
  - task: coco_learning_tasks

hydra:
  run:
    dir: ${output_dir}/${exp_name}